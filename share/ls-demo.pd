\ [[ -n $PERFECT_DEMO_ROOT ]] || die 'PERFECT_DEMO_ROOT not defined'
\ [[ $(uname) == Darwin ]] || die 'This demo only works in OSX'

\ PERFECT_DEMO_PROMPT="Perfect Demo for 'ls' > "
\ PERFECT_DEMO_WAIT=1
\ PERFECT_DEMO_PAUSE=1

$# Welcome to the Perfect Demo for the 'ls' command:

# First let's cd to the perfect-demo repo directory.
$ cd $PERFECT_DEMO_ROOT
\ c

# Now we will use the 'ls' command to see all the files and subdirectories.
$ ls
\ c

# We can show even more info with the minus el flag.
$ ls -l
\ c

$# THE END
